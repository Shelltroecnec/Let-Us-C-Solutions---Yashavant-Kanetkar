/**** Rewrite the Following Programs Using Conditional Operator ********
/* [J] - (a) */

#include<stdio.h>
int main(){
	int x, min, max;
	scanf("%d %d",&max, &x);
	x =(x > max ? max : min);
}
